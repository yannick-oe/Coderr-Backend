"""Application configuration for the base_info_app app."""

from django.apps import AppConfig


class BaseInfoAppConfig(AppConfig):
    """Configure the base_info_app application."""

    name = "base_info_app"
