"""Application configuration for the reviews_app app."""

from django.apps import AppConfig


class ReviewsAppConfig(AppConfig):
    """Configure the reviews_app application."""

    name = "reviews_app"
