"""Application configuration for the offers_app app."""

from django.apps import AppConfig


class OffersAppConfig(AppConfig):
    """Configure the offers_app application."""

    name = "offers_app"
