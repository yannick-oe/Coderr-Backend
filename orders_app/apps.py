"""Application configuration for the orders_app app."""

from django.apps import AppConfig


class OrdersAppConfig(AppConfig):
    """Configure the orders_app application."""

    name = "orders_app"
